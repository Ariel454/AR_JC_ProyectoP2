<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="AR_JC_ProyectoP2.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:AR_JC_ProyectoP2"
    Shell.FlyoutBehavior="Disabled">

    <ShellContent
            Title="Ingresar"
            ContentTemplate="{DataTemplate local:Login}"
            Route="Login" />
    
    
    <ShellContent
            Title="Menu"
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />

    <TabBar>
            <ShellContent
            Title="Añadir peliculas"
            ContentTemplate="{DataTemplate local:CrearPelicula}"
            Route="CrearPelicula" />
            
            <ShellContent
            Title="Gestionar peliculas"
            ContentTemplate="{DataTemplate local:GestionarPeliculas}"
            Route="GestionarPeliculas" />
    </TabBar>

    <ShellContent
            Title="Registrarse"
            ContentTemplate="{DataTemplate local:RegisterPage}"
            Route="RegisterPage" />


</Shell>
